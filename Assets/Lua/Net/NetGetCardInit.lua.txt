NetGetCardInit = {}
local Net = require("Net/Base/NetGetCardInitBase")
Net.__index = Net

function NetGetCardInit.Create(callback)
    local t = {}
	setmetatable(t, Net)
    t:Init(callback)
    return t
end 

function Net:OnResult(v)
         self.data=v.value.data
end
