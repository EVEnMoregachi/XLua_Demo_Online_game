NetTimeLookBoxPrize = {}
local net = require("Net/Base/NetTimeLookBoxPrizeBase")
net.__index = net

function NetTimeLookBoxPrize.Create(boxId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(boxId,callback)
    return t
end 

function net:OnResult(v)
       XunXiaTime.BoxData = {} 
       XunXiaTime.BoxData = v.value.data["timeLookBoxState"]  
end
