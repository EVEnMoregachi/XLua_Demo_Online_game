NetCaiShenGo = {}
local net = require("Net/Base/NetCaiShenGoBase")
net.__index = net

function NetCaiShenGo.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
