NetEmailRead = {}
local net = require("Net/Base/NetEmailReadBase")
net.__index = net

function NetEmailRead.Create(emailId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(emailId,callback)
    return t
end 

function net:OnResult(v)
          social.emailMsgInIt = v.value.data.emailMsgInIt  
end
