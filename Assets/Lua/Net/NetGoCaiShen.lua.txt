NetGoCaiShen = {}
local net = require("Net/Base/NetGoCaiShenBase")
net.__index = net

function NetGoCaiShen.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        self.datav = v.value.data
end
