NetGetHeroSoulInit = {}
local net = require("Net/Base/NetGetHeroSoulInitBase")
net.__index = net

function NetGetHeroSoulInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
