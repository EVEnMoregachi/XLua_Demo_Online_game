NetGetWulinPuInIt = {}
local net = require("Net/Base/NetGetWulinPuInItBase")
net.__index = net

function NetGetWulinPuInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
    collect.SxData = {} 
    collect.SxData = v.value.data["wuLinPuAddNature"]  
    
end
