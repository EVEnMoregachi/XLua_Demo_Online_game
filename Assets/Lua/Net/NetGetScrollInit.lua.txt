NetGetScrollInit = {}
local net = require("Net/Base/NetGetScrollInitBase")
net.__index = net

function NetGetScrollInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
