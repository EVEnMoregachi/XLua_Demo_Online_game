NetGetSkyBookInIt = {}
local net = require("Net/Base/NetGetSkyBookInItBase")
net.__index = net

function NetGetSkyBookInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
           skybook.skyBookInItList =  v.value.data.skyBookInItList

end
