NetHeroWuXueStateLift = {}
local net = require("Net/Base/NetHeroWuXueStateLiftBase")
net.__index = net

function NetHeroWuXueStateLift.Create(heroId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(heroId,callback)
    return t
end 

function net:OnResult(v)
        
end
