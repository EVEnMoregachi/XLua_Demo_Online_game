NetGetMentInIt = {}
local net = require("Net/Base/NetGetMentInItBase")
net.__index = net

function NetGetMentInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v) 
      master.mentMsgInIt = v.value.data.mentMsgInIt
end
