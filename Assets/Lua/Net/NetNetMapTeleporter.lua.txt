NetNetMapTeleporter = {}
local net = require("Net/Base/NetNetMapTeleporterBase")
net.__index = net

function NetNetMapTeleporter.Create(map_id,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(map_id,callback)
    return t
end 

function net:OnResult(v)
        
end
