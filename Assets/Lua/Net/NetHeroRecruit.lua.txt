NetHeroRecruit = {}
local net = require("Net/Base/NetHeroRecruitBase")
net.__index = net

function NetHeroRecruit.Create(heroId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(heroId,callback)
    return t
end 

function net:OnResult(v)
        
end
