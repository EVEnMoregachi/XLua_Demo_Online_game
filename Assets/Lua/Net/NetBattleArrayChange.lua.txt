NetBattleArrayChange = {}
local net = require("Net/Base/NetBattleArrayChangeBase")
net.__index = net

function NetBattleArrayChange.Create(heroId,posid,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(heroId,posid,callback)
    return t
end 

function net:OnResult(v)
        
end
