NetFriendToast = {}
local net = require("Net/Base/NetFriendToastBase")
net.__index = net

function NetFriendToast.Create(playerId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(playerId,callback)
    return t
end 

function net:OnResult(v)
        
end
