NetGetXiaKeDaoInit = {}
local net = require("Net/Base/NetGetXiaKeDaoInitBase")
net.__index = net

function NetGetXiaKeDaoInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        self.XiakeDaoData = v.value.data
end
