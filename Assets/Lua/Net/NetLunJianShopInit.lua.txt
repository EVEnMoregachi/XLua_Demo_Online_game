NetLunJianShopInit = {}
local net = require("Net/Base/NetLunJianShopInitBase")
net.__index = net

function NetLunJianShopInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
    lunjian.ShoppInItInfo = {} 
    lunjian.ShoppInItInfo = v.value.data["lunJianShopList"]  --论剑列表信息 
    lunjian.lunJianScore  = v.value.data.lunJianScore  --论剑积分  
end
