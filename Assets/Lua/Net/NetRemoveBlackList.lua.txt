NetRemoveBlackList = {}
local net = require("Net/Base/NetRemoveBlackListBase")
net.__index = net

function NetRemoveBlackList.Create(playerId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(playerId,callback)
    return t
end 

function net:OnResult(v)
        
end
