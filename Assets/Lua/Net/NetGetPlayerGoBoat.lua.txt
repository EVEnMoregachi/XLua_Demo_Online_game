NetGetPlayerGoBoat = {}
local net = require("Net/Base/NetGetPlayerGoBoatBase")
net.__index = net

function NetGetPlayerGoBoat.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        self.XingZhouData = v.value.data
end
