NetGetRetreatInIt = {}
local net = require("Net/Base/NetGetRetreatInItBase")
net.__index = net

function NetGetRetreatInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        self.BiGuanData = v.value.data
end
