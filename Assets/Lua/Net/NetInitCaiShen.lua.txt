NetInitCaiShen = {}
local net = require("Net/Base/NetInitCaiShenBase")
net.__index = net

function NetInitCaiShen.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
