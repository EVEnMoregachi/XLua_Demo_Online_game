NetGetMapDaoJuInIt = {}
local net = require("Net/Base/NetGetMapDaoJuInItBase")
net.__index = net

function NetGetMapDaoJuInIt.Create(map_id,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(map_id,callback)
    return t
end 

function net:OnResult(v)
        
end
