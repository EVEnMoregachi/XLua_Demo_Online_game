NetGetPersonMsg = {}
local net = require("Net/Base/NetGetPersonMsgBase")
net.__index = net

function NetGetPersonMsg.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        master.personMsg = v.value.data.personMsg
end
