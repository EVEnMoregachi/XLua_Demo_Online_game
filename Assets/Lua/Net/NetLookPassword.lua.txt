NetLookPassword = {}
local net = require("Net/Base/NetLookPasswordBase")
net.__index = net

function NetLookPassword.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
     backpack.secretKey = v.value.data.secretKey    
end
