NetGetHerartInIt = {}
local net = require("Net/Base/NetGetHerartInItBase")
net.__index = net

function NetGetHerartInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
     herart.PetHeadList = v.value.data.herartCardList
  end
