NetAddLogGetPrize = {}
local net = require("Net/Base/NetAddLogGetPrizeBase")
net.__index = net

function NetAddLogGetPrize.Create(prize_id,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(prize_id,callback)
    return t
end 

function net:OnResult(v)
                welfare.WelfareMsg = v.value.data.welfareMsg 
         welfare.WelfareAddLogInIt = v.value.data.welfareAddLogInIt 
 end
