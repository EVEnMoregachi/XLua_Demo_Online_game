NetFriendCall = {}
local net = require("Net/Base/NetFriendCallBase")
net.__index = net

function NetFriendCall.Create(playerId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(playerId,callback)
    return t
end 

function net:OnResult(v)
        
end
