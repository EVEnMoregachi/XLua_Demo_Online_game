NetGetMapTaskInIt = {}
local net = require("Net/Base/NetGetMapTaskInItBase")
net.__index = net

function NetGetMapTaskInIt.Create(map_id,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(map_id,callback)
    return t
end 

function net:OnResult(v)
        
end
