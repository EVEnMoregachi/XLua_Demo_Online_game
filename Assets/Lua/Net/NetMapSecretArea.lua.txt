NetMapSecretArea = {}
local net = require("Net/Base/NetMapSecretAreaBase")
net.__index = net

function NetMapSecretArea.Create(map_id,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(map_id,callback)
    return t
end 

function net:OnResult(v)
        
end
