NetAddBlackList = {}
local net = require("Net/Base/NetAddBlackListBase")
net.__index = net

function NetAddBlackList.Create(playerId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(playerId,callback)
    return t
end 

function net:OnResult(v)
        
end
