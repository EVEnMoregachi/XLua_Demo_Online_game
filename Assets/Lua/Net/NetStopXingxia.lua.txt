NetStopXingxia = {}
local net = require("Net/Base/NetStopXingxiaBase")
net.__index = net

function NetStopXingxia.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
