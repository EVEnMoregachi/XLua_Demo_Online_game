NetGetEquipSoulInit = {}
local net = require("Net/Base/NetGetEquipSoulInitBase")
net.__index = net

function NetGetEquipSoulInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
