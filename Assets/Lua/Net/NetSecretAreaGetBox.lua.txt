NetSecretAreaGetBox = {}
local net = require("Net/Base/NetSecretAreaGetBoxBase")
net.__index = net

function NetSecretAreaGetBox.Create(map_id,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(map_id,callback)
    return t
end 

function net:OnResult(v)
        
end
