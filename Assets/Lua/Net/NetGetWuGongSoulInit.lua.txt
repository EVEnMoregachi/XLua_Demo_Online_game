NetGetWuGongSoulInit = {}
local net = require("Net/Base/NetGetWuGongSoulInitBase")
net.__index = net

function NetGetWuGongSoulInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
