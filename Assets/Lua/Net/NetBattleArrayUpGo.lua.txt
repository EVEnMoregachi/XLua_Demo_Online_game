NetBattleArrayUpGo = {}
local net = require("Net/Base/NetBattleArrayUpGoBase")
net.__index = net

function NetBattleArrayUpGo.Create(heroId,posid,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(heroId,posid,callback)
    return t
end 

function net:OnResult(v)
        
end
