NetGetBigMapInIt = {}
local net = require("Net/Base/NetGetBigMapInItBase")
net.__index = net

function NetGetBigMapInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
