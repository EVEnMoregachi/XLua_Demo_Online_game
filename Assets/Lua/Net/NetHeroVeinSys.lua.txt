NetHeroVeinSys = {}
local net = require("Net/Base/NetHeroVeinSysBase")
net.__index = net

function NetHeroVeinSys.Create(heroId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(heroId,callback)
    return t
end 

function net:OnResult(v)
        
end
