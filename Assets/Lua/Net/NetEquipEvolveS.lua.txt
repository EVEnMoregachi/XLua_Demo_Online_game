NetEquipEvolveS = {}
local net = require("Net/Base/NetEquipEvolveSBase")
net.__index = net

function NetEquipEvolveS.Create(equipId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(equipId,callback)
    return t
end 

function net:OnResult(v)
     self.datav = v.value.data
end
