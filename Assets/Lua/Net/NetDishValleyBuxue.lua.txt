NetDishValleyBuxue = {}
local net = require("Net/Base/NetDishValleyBuxueBase")
net.__index = net

function NetDishValleyBuxue.Create(heroId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(heroId,callback)
    return t
end 

function net:OnResult(v)
        
end
