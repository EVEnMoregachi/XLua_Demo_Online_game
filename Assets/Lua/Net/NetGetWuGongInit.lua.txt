NetGetWuGongInit = {}
local net = require("Net/Base/NetGetWuGongInitBase")
net.__index = net

function NetGetWuGongInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
