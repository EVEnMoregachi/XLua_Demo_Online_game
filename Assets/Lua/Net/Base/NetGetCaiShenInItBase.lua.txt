local NetGetCaiShenInItBase = {}

--财神到初始化
function NetGetCaiShenInItBase:Init(callback)
    self.data = {}
	self.data.msgName = "GetCaiShenInIt"

	self.callback = callback

    return self
end

function NetGetCaiShenInItBase:Send()
    Net.Send(self)
end

function NetGetCaiShenInItBase:OnReceive(v)
	-- body
    self.responseData = v.value.data[self.data.msgName]
    self:OnResult(v)
    if self.callback ~= nil then
        self.callback(self)
    end
end

return NetGetCaiShenInItBase

