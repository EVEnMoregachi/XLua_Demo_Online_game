local NetGetTavernShoppInItBase = {}

--商贾初始化
function NetGetTavernShoppInItBase:Init(callback)
    self.data = {}
	self.data.msgName = "GetTavernShoppInIt"

	self.callback = callback

    return self
end

function NetGetTavernShoppInItBase:Send()
    Net.Send(self)
end

function NetGetTavernShoppInItBase:OnReceive(v)
	-- body
    self.responseData = v.value.data[self.data.msgName]
    self:OnResult(v)
    if self.callback ~= nil then
        self.callback(self)
    end
end

return NetGetTavernShoppInItBase

