NetXiaKeDaoInit = {}
local net = require("Net/Base/NetXiaKeDaoInitBase")
net.__index = net

function NetXiaKeDaoInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
