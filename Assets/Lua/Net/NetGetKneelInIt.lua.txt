NetGetKneelInIt = {}
local net = require("Net/Base/NetGetKneelInItBase")
net.__index = net

function NetGetKneelInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        self.canbaiData = v.value.data
end
