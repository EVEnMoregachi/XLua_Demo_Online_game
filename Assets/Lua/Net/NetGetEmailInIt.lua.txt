NetGetEmailInIt = {}
local net = require("Net/Base/NetGetEmailInItBase")
net.__index = net

function NetGetEmailInIt.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
    social.emailMsgInIt = v.value.data.emailMsgInIt  


end
