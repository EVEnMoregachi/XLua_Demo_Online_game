NetEmailGetPrize = {}
local net = require("Net/Base/NetEmailGetPrizeBase")
net.__index = net

function NetEmailGetPrize.Create(emailId,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(emailId,callback)
    return t
end 

function net:OnResult(v)
         social.emailMsgInIt = v.value.data.emailMsgInIt   
end
