NetMapLighten = {}
local net = require("Net/Base/NetMapLightenBase")
net.__index = net

function NetMapLighten.Create(map_id,callback)
    local t = {}
	setmetatable(t, net)
    t:Init(map_id,callback)
    return t
end 

function net:OnResult(v)
        
end
