NetGetPublicMsgInit = {}
local net = require("Net/Base/NetGetPublicMsgInitBase")
net.__index = net

function NetGetPublicMsgInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
