NetGetPackageInit = {}
local net = require("Net/Base/NetGetPackageInitBase")
net.__index = net

function NetGetPackageInit.Create(callback)
    local t = {}
	setmetatable(t, net)
    t:Init(callback)
    return t
end 

function net:OnResult(v)
        
end
